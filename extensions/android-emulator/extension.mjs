// Plugin extension paths are containers. Load the canonical source extension
// so marketplace and repository installs share one implementation.
import "../../.github/extensions/android-emulator/extension.mjs";
